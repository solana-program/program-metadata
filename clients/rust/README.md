# Rust client

A generated Rust library for the Program Metadata program.

## Getting started

To build and test your Rust client from the root of the repository, you may use the following command.

```sh
make test-clients-rust
```

This will run the tests for your Rust client against the program built into `target/deploy` (run `make build-sbf-program` first).
